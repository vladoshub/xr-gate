diff --git a/CMakeLists.txt b/CMakeLists.txt
index b827394..a47312b 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -366,6 +366,17 @@ basalt_add_gui_link_options(basalt_calibrate_imu)
 basalt_add_macos_runtime_paths(basalt_calibrate_imu)
 
 
+set(XR_TRACKING_ROOT "$ENV{XR_TRACKING_ROOT}" CACHE PATH "Path to xr_tracking checkout")
+if(XR_TRACKING_ROOT AND EXISTS "${XR_TRACKING_ROOT}/backends/basalt_vio/CMakeLists.txt")
+  message(STATUS "Enabling XR Basalt VIO backend from ${XR_TRACKING_ROOT}/backends/basalt_vio")
+  add_subdirectory(
+    "${XR_TRACKING_ROOT}/backends/basalt_vio"
+    "${CMAKE_BINARY_DIR}/xr_tracking/backends/basalt_vio"
+  )
+else()
+  message(STATUS "XR Basalt VIO backend disabled; set XR_TRACKING_ROOT to the xr_tracking checkout")
+endif()
+
 add_executable(basalt_vio_sim src/vio_sim.cpp)
 target_link_libraries(basalt_vio_sim basalt ${BASALT_PANGO_TARGETS} basalt::cli11)
 basalt_add_gui_link_options(basalt_vio_sim)
diff --git a/vcpkg.json b/vcpkg.json
index e36dd6b..f84e3df 100644
--- a/vcpkg.json
+++ b/vcpkg.json
@@ -38,13 +38,6 @@
         "png"
       ]
     },
-    {
-      "name": "realsense2",
-      "features": [
-        "rs-usb-backend",
-        "tm2"
-      ]
-    },
     "tbb",
     "basalt-headers",
     "opengv",
