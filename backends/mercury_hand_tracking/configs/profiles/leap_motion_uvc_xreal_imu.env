# Leap Motion UVC cameras with the glasses/XREAL IMU. Mercury consumes only
# camera0/camera1, so it uses the same camera calibration as camera-only Leap.
MERCURY_CALIB="${MERCURY_CALIB:-$HOME/.config/xr_tracking/calibration/leap_uvc/mercury_leap_test_640x480.json}"
