# Third-party notices for MCU tools

The MCU tools use a vendored snapshot of `TheJackiMonster/nrealAirLinuxDriver`:

```text
third_party/nrealAirLinuxDriver
```

The upstream project is distributed under the MIT License. Keep the upstream
copyright and license notice when copying, modifying, building or distributing
these files:

```text
third_party/nrealAirLinuxDriver/LICENSE.md
```

In XR Tracking this code is isolated as an explicit, opt-in toolchain
for MCU experiments only. It is not used by the default runtime pipeline.
